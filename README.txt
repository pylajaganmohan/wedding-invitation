Sasidhar & Vijaya — Final 1 Minute Wedding Invitation

Previous premium maroon/gold template retained.
60-second, 10-scene timeline.
Original photo1-photo3 plus five new portrait images photo4-photo8.
New images are 1080x1920 portrait canvases with the complete uploaded image preserved over a soft blurred background.
Play/Pause controls both invitation animation and audio.
Mute/Unmute controls audio only and resynchronizes when unmuted.
Save Date provides Google Calendar / Apple Calendar.
Ready for GitHub Pages.

Important: the added image variables use unique names so they do not conflict with React's internal minified variables.

Timeline seeking: clicking or dragging the progress bar now synchronizes the audio to the same timestamp.

Share button update:
- Shares a short wedding invitation message plus the current GitHub Pages URL.
- The URL is taken automatically from window.location, so no manual URL editing is required after GitHub Pages deployment.
- On unsupported browsers, the complete message and URL are copied to the clipboard.

SHARE URL
- Live Preview/localhost URLs are NOT shared anymore.
- During local testing, Share uses the placeholder:
  https://YOUR-USERNAME.github.io/YOUR-REPOSITORY/
- Replace that placeholder in index.html with your actual GitHub Pages URL if you want to test sharing before deployment.
- Once the site is opened from GitHub Pages, the current GitHub Pages URL is detected automatically.
